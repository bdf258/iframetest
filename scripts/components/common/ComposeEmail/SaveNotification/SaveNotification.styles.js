import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  saveDraftNotification: {
    color: "#363636",
    fontSize: "90%",
    margin: 0,
    textAlign: "center",
    padding: "0 0.5em",
  },
});
