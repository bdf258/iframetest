import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  spinner: {
    width: "100%",
    textAlign: "center",
  },
  error: {
    color: "red",
  },
  dropHereText:{
    padding: '200px 100px',
  }
});
