import { createUseStyles } from "react-jss";

export default createUseStyles({
  customFieldManagerContainer: {
    display: "flex",
    minWidth: 920,
    marginTop: 20,
    flexDirection: "column",
  },
});
