import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  label: {
    minWidth: 370,
  },
  inputContainer: {
    margin: { top: 0, left: 0, right: 0, bottom: 2 },
  },
  crossIconContainer: {
    marginTop: 4,
    marginLeft: 2,
  },
});
