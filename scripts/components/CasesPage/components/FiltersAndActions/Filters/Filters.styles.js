import { createUseStyles } from "react-jss";

const useStyles = createUseStyles({
  placeholder: { margin: { bottom: 10, right: 15 } },
  filters: {
    margin: {
      right: 10,
      bottom: 5,
    },
  },
  customFieldsHeading: { margin: { left: 5, right: 5 } },
});

export default useStyles;
