import { createUseStyles } from "react-jss";

const useStyles = createUseStyles({
  autoCompleteInput: { textAlign: "center" },
  bulkActionsContainer: { width: 650 },
  buttonRow: { marginTop: "1em" },
  letterTemplateSelectContainer: {
    margin: { top: 0, right: 0, bottom: 16, left: 0 },
  },
  letterTemplateSelectLabel: { width: 125 },
});

export default useStyles;
