import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  orderSelectWrapper: {
    minWidth: 165,
    display: "inherit",
    alignItems: "inherit",
    flex: 1,
  },
  label: { margin: { right: 8 } },
  formSelectContainer: {
    margin: 0,
  },
});
