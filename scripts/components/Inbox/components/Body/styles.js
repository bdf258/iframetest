import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  inboxBody: {
    padding: 0,
    minHeight: 450,
    overflow: "hidden",
    display: "flex",
    height: "100vh",
  },
  section: {
    flex: "0 1",
  },
});
