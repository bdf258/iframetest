import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  button: {
    fontSize: "0.8rem",
    padding: 4,
    margin: 8,
    height: 25,
    // button reset
    overflow: "visible",
    color: "inherit",
    font: "inherit",
    lineHeight: "16px",
    outline: "none",
  },
});
