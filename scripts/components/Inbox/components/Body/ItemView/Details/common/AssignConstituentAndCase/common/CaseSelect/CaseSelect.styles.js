import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  caseSelectContainer: {
    display: "flex",
    justifyContent: "center",
    margin: {
      bottom: "1rem",
    },
  },
  selectContainer: {
    maxWidth: 385,
    margin: 0,
  },
});
