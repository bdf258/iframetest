import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  centerText: {
    textAlign: "center",
  },
  textButton: {
    fontSize: "100%",
  },
  underlineText: {
    textDecoration: "underline",
  },
});
