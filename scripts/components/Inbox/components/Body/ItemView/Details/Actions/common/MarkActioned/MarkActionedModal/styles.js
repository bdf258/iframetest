import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  modal: { maxWidth: 500, textAlign: "center" },
  buttons: {
    display: "flex",
    justifyContent: "space-between",
  },
});
